# 고운 (GOUN) 프로젝트 메모

이 파일은 다음 세션에서도 자동으로 읽혀서 기억을 이어가기 위한 메모입니다.

## 향후 아이디어 / TODO

- **정리 진행 중 (다듬기 작업)**: "처음 생각했던 거랑 너무 달라졌다"는 피드백으로 앱 전체를 훑어서 가짜/미완성 버튼 정리 시작함. 완료: 화장 배틀 기능 전체 삭제(홈 배너, view-battle, 관련 JS/CSS 다 제거), 홈 화면 성분 스캐너 배너 제거(view-scanner 코드는 남아있음, 진입점만 없앰), "올리브영" 문구를 "쿠팡"으로 수정(구매하러 가기 버튼, 플레이어 화면 구매 버튼). 아직 남은 가짜 버튼: 프리미엄 구독 결제(가짜 Stripe 문구), "오늘 룩 저장하기"(색상진단 화면), 광고 문의/결제(사업자등록 전까지는 의도적으로 미룸). 다음에 이어서 정리할 것.

- **플레이어 화면 광고 삽입**: 영상 재생 화면 왼쪽의 빈 공간(아바타 옆)에, 나중에 광고주가 확보되면 광고를 넣는 방안을 고려하기로 함. (제품 태그 기능은 검토했으나, "이 화장법 따라해보기" → 뷰티랩 화면에서 이미 제품/색상 정보를 다룰 수 있어서 넣지 않기로 결정함.) 마이페이지의 "광고 문의하기" 메뉴가 이미 광고주 온보딩 창구 역할을 하고 있음.

## 발라보기(가상 메이크업 AR) — 현재 숨김 처리됨

`components/virtualTryOn.js` (MediaPipe FaceLandmarker 기반, 무료)로 얼굴 사진에 립/아이/블러셔 색을 입히는 기능을 만들었지만, 사용자가 실제 폰으로 테스트해보고 "퀄리티가 너무 허접하다"고 판단해서 **뷰티랩 화면의 진입점(발라보기 버튼, 룩 프리셋 섹션)을 `hidden` 클래스로 숨겨뒀음** (코드는 그대로 남겨둠, `components/gounMarkup.js`에서 `#try-selected-btn`과 `#look-preset-section`에 `hidden` 클래스만 추가한 상태).

**결론 확정됨**: 버그(여러 제품 배열 처리 오류)를 고치고, color 블렌드 모드 + 진한 채도로 제대로 다시 테스트했는데도 사용자가 "아닌 것 같다"고 판단함. 즉 MediaPipe 랜드마크 + 캔버스 색칠 방식 자체의 한계로 결론 남 (단순 버그가 아님). 더 이상 이 방식으로 화질 개선 시도하지 말 것.

다음에 이걸 다시 켜려면:
1. Perfect Corp YouCam API(유료, 월 $24~32부터, docs.perfectcorp.com) 같은 상용 AI 서비스로 교체해서 퀄리티를 올리거나
2. 지금 무료 버전을 그대로 다시 노출하고 싶다면 `components/gounMarkup.js`의 `#try-selected-btn`과 `#look-preset-section`에서 `hidden` 클래스만 제거하면 됨

사용자가 유료 API로 전환할지 결정하기 전까지는 계속 숨겨둘 것.

## 비즈니스 모델 확정 사항

- **포인트 가치: 1P = 1원으로 확정됨.** "현금 전환"/"기프티콘 교환" 버튼(마이페이지)은 아직 실제 로직 없는 가짜 버튼인데, 나중에 진짜로 만들 때 이 비율(1P=1원)로 환산해야 함.
- **친구 초대 리워드**: 초대한 사람 500P(500원), 초대받은 친구 +300P(300원, 기존 가입 웰컴 500P에 추가) = 성공당 총 800원 비용. 업계 CAC(고객획득비용) 대비 저렴하다고 판단해서 확정함. `components/gounAppLogic.js`의 `maybeRewardReferral()`에 구현되어 있고, 위시리스트 첫 활동 시 지급됨.
- **AI 고화질 티켓(Perfect Corp API 연동 예정) 가격**: A안(건별 990원) 확정. 원가 추정 150~250원/회 대비 마진 75~85%. 무료 2회 제공 후 3회차부터 유료. 묶음(5장 4,500원, 10장 8,000원)도 고려 중이나 아직 미구현 — Perfect Corp API 자체가 아직 연동 전이라 실제 결제 로직도 미구현 상태.
- **크리에이터 커미션은 B안(고운 통합 정산)으로 확정**: 고운이 사업자로 쿠팡파트너스 가입 → 크리에이터별 구분 링크(subid) 발급 → 판매 실적을 고운이 받아서 크리에이터 등급별(파워 크리에이터 45% 등, 이미 마이페이지 UI에 있음)로 정산. A안(크리에이터 개인 정산)은 고운에 수익이 전혀 안 남는 구조라 기각됨.
- **사업자 등록이 세 가지 수익화를 동시에 막고 있음**: AI 티켓 결제(PG사 연동), 크리에이터 커미션 정산(B안), 쿠팡파트너스 통합 가입. 개인사업자로 낼 예정. **사용자가 먼저 얘기 꺼내기 전까지는 먼저 권하지 말 것** — "지금은 고운 웹을 다듬어야 한다"며 보류함.
